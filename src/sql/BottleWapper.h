#pragma once
#include "PgsqlHelper.h"
#include "Utils.h"
#include "json/json.h"

class BottleWapper
{
  private:
    /* data */
  public:
    BottleWapper() = default;
    ~BottleWapper() = default;
    static bool addBottle(const Json::Value &jsonData)
    {
        bool res = false;

        return res;
    };
};
